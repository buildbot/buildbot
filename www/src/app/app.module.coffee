# first thing, we install underscore.string inside lodash
_.mixin(_.str.exports())

class App extends App
    constructor: -> return [
        'ngAnimate'
        'ui.bootstrap'
        'ui.router'
        'restangular'
        'RecursionHelper'
    ]
