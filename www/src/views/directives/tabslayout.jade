div
    tabset
        tab(ng-repeat='field in field.fields | filter:{hide:false}',
            heading="{{field.name}}",
            class="{{column_class}}")
            forcefield(recursive,
                       field="field")
