Fixed the "Responsible Users" tab of the build view to show users from the ``owner`` and ``owners`` build properties. Previously owner information was only considered for builds started by a scheduler named exactly ``force``, and only when the ``owner`` property was in ``Name <email>`` format, so e.g. forced builds with a plain email address showed no responsible users (:issue:`3476`).
