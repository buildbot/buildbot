Workers view paginate workers list (page size can be configured with the setting 'Workers page related settings > Number of workers to show per page').
A search box was also added to filter on worker's name.