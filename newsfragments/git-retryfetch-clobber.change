The :bb:step:`Git` step now clobbers and re-clones when ``retryFetch=True`` and ``clobberOnFailure=True`` are combined and the retried fetch also fails. Previously the retry's failure ended the step without honoring ``clobberOnFailure``.
