/*global define, describe, it, expect, beforeEach, afterEach*/
define(["jquery"], function ($) {
    "use strict";

    describe("Time elements", function () {
        it("should fail", function () {
            expect(0).toEqual(1);
        });
    });
});